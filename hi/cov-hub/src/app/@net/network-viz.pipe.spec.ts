import { NetworkVizPipe } from './network-viz.pipe';

describe('NetworkVizPipe', () => {
  it('create an instance', () => {
    const pipe = new NetworkVizPipe();
    expect(pipe).toBeTruthy();
  });
});
