import { DiabetesEntryDirective } from './diabetes-entry.directive';

describe('DiabetesEntryDirective', () => {
  it('should create an instance', () => {
    const directive = new DiabetesEntryDirective();
    expect(directive).toBeTruthy();
  });
});
