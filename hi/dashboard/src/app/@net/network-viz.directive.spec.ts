import { NetworkVizDirective } from './network-viz.directive';

describe('NetworkVizDirective', () => {
  it('should create an instance', () => {
    const directive = new NetworkVizDirective();
    expect(directive).toBeTruthy();
  });
});
