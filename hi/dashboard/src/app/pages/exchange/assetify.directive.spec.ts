import { AssetifyDirective } from './assetify.directive';

describe('AssetifyDirective', () => {
  it('should create an instance', () => {
    const directive = new AssetifyDirective();
    expect(directive).toBeTruthy();
  });
});
