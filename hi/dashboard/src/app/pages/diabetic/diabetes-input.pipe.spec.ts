import { DiabetesInputPipe } from './diabetes-input.pipe';

describe('DiabetesInputPipe', () => {
  it('create an instance', () => {
    const pipe = new DiabetesInputPipe();
    expect(pipe).toBeTruthy();
  });
});
