import { PasswordAuthStrategyOptions } from './password-auth-strategy-options';

describe('PasswordAuthStrategyOptions', () => {
  it('should create an instance', () => {
    expect(new PasswordAuthStrategyOptions()).toBeTruthy();
  });
});
