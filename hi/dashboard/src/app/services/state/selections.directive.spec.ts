import { SelectionsDirective } from './selections.directive';

describe('SelectionsDirective', () => {
  it('should create an instance', () => {
    const directive = new SelectionsDirective();
    expect(directive).toBeTruthy();
  });
});
