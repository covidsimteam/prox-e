import { CompDirective } from './comp.directive';

describe('CompDirective', () => {
  it('should create an instance', () => {
    const directive = new CompDirective();
    expect(directive).toBeTruthy();
  });
});
