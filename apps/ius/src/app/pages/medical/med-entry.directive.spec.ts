import { MedEntryDirective } from './med-entry.directive';

describe('MedEntryDirective', () => {
  it('should create an instance', () => {
    const directive = new MedEntryDirective();
    expect(directive).toBeTruthy();
  });
});
