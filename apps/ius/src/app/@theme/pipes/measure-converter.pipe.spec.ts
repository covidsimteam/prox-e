import { MeasureConverterPipe } from './measure-converter.pipe';

describe('MeasureConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new MeasureConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
