import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root',
})
export class DBCommonsService {
  // TODO add repeated code from pcr, rdt, returnee and possibly others here.
  constructor() { }
}
